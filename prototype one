#include <iostream>
#include<cmath>

int main()
{ 
    double a;
    double b;
    std::cout<<"Enter the adjacent: ";
    std::cin>> a;
    std::cout<<"Enter the opposite: ";
    std::cin>> b;
    
    double c = (pow(a, 2)) + (pow(b, 2));
    std::cout<<sqrt(c);

    return 0;
}
